# Disable source maps to reduce file count
GENERATE_SOURCEMAP=false

# Minimize file names to avoid long path issues
REACT_APP_MINIMIZE_FILE_NAMES=true

# Ensure we're using production mode
NODE_ENV=production

# For Render, we need to use the root path
PUBLIC_URL=/

# Backend API URL for production
REACT_APP_API_URL=https://pay-backend-iu9e.onrender.com 