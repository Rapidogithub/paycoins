# Disable source maps to reduce file count
GENERATE_SOURCEMAP=false

# Minimize file names to avoid long path issues
REACT_APP_MINIMIZE_FILE_NAMES=true

# Ensure we're using production mode
NODE_ENV=production

# Add base path for GitHub Pages
PUBLIC_URL=https://rapidogithub.github.io/paycoins/

# Backend API URL for production
# Make sure this URL points to your Railway deployment with no port specified
# Railway handles the port mapping internally
REACT_APP_API_URL=https://pay-coins.onrender.com